term : type.

axiom : string -> term -> term.

typ : term.
set : term.

forall : term -> (term -> term) -> term.
lam : term -> (term -> term) -> term.
app : term -> term -> term.

(* Some basic integer extras *)
zobject : term.
z : int -> term.
add : term.
